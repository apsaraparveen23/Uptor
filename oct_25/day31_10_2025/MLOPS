ML-OPS is nothing but its a complete life cycle of a model that covers from basic model
selection to model deployment
Stage1:
     Programming
Stage2:(Core)(i.e.,important)
     Algorithm
     Model evaluation
     preprocess
Stage3:(Devops and cloud)
      Github(project versioning)
      CI/CD(continuous integration and deployment)-Jenkins
      Cloud platforms(any platform like amazon,azure etc)
Stage4:Data engineering
      SQL or no-SQL
      ETL(Extract transform and load) (Power BI)
      Data Versioning
Data Versioning:
     Data Versioning can be achieved with various sources / tools
     1.DVC(Data version control)
       1.1 Data sets are tracked based upon unique hashes(checksum or encryption)
     2.MLflow
       2.1 ML flow -It allows helps us in tracking,logging any many more.

DVC(Data version control):
Installation:
pip install dvc

SQL installation:
1.SQL(structured query language)
 1.1 Database can be broadly divided in to two categories
     i SQL
       My SQL,MS server,Oracle

     ii No-SQL(no structured)
        MONGO DB -doesnt have a structure
2.MY SQL
  2.1 communication with MYSQL database can be made in the following option.
     2.1.1 Through CLI(Command line interface)
     2.1.2 through Work bench(Client).
     2.1.3 Through programming language(python)
  2.2 Communication with python
      Installation----pip install mysql.connector-python/pip install mysql-connector-python
      Execution
             Execution can be done through queries
             Queries can be classified in to following:
               * DML(Data Manipulation Language)
               * DDL(Data Descriptive Language)
               * DQL(Data Query Language)
      Flow of execution:

               1.connect to the database
               2.Select the table and Run

            Sample execution commands:
                  1.show databases;
























Different distance based algorithm

1.These algorithms rely on measuring similarity or dissimilarity between data points
using distance metrics like Euclidean, Manhattan, or Cosine.
2. Popular Distance-Based Algorithm
i)K-Nearest Neighbors (KNN)
ii)K-Means Clustering
iii)Hierarchical Clustering
iv)DBSCAN (Density-Based Spatial Clustering)
V)Radius Neighbors Classifier



